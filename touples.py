# Touples
# like list but are immutable
# meaning it can not be changed
# use [] for list and () for touples

numbers = (1,2,3,4, 4, 4, 4, 5)
print(numbers)
print(numbers.count(4))
print(numbers.index(4))